/**
 * 
 * @param {Docker} docker 
 * @returns List of containers 
 */
const list_containers = async (docker)=> {
    return;
};
/**
 * 
 * @param {Container} container 
 * @returns 0 on succes, -1 on error
 */
const run_container = (container) =>{
    return;
}
/**
 * 
 * @param {Container} container 
 * @returns 0 on success, -1 on error
 */
const stop_container = (container)=>{
    return;
}

module.exports ={list_containers,run_container,stop_container};